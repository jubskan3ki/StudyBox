// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// PAGE PAS UTILISE A IMPLENTER UNE FOIS APP MOBILE ET DATA GENERER
// interface Event {
//   id: number;
//   title: string;
// }

const UserEventsPage = () => {
    // useEffect(() => {
    //   const fetchEvents = async () => {
    //     try {
    //       const data = await getAllEvents();
    //       setEvents(data);
    //     } catch (error) {
    //       console.error(error);
    //     }
    //   };

    //   fetchEvents();
    // }, []);

    return (
        <div>
            <h1>Mes User</h1>
            <ul>
                {/* {events.map((event) => (
          <li key={event.id}>{event.title}</li>
        ))} */}
                Futur besoin de l'app
            </ul>
        </div>
    );
};

export default UserEventsPage;
